@model List<KampusKodu.Models.AppUser>

@{
    ViewData["Title"] = "Liderlik Tablosu";
    int rank = 1;
}

<div class="container py-5">
    <div class="text-center mb-5">
        <h1 class="display-5 fw-bold text-warning"><i class="bi bi-trophy-fill"></i> Liderlik Tablosu</h1>
        <p class="lead text-muted">Kampüsün en aktif öğrencileri burada yarışıyor!</p>
    </div>

    <div class="card border-0 shadow-lg">
        <div class="card-body p-0">
            <table class="table table-hover mb-0">
                <thead class="bg-light">
                    <tr>
                        <th class="p-4">#</th>
                        <th class="p-4">Öğrenci</th>
                        <th class="p-4 text-end">Puan</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var user in Model)
                    {
                        var rowClass = rank switch
                        {
                            1 => "table-warning fw-bold",
                            2 => "table-light fw-bold",
                            3 => "table-light fw-bold",
                            _ => ""
                        };

                        var icon = rank switch
                        {
                            1 => "🥇",
                            2 => "🥈",
                            3 => "🥉",
                            _ => rank + "."
                        };

                        <tr class="@rowClass">
                            <td class="p-4 align-middle fs-5">@icon</td>
                            <td class="p-4 align-middle">
                                <a asp-action="Profile" asp-route-id="@user.Id" class="text-decoration-none text-dark d-flex align-items-center gap-2">
                                    <i class="bi bi-person-circle fs-4 text-secondary"></i>
                                    @user.FullName
                                </a>
                            </td>
                            <td class="p-4 align-middle text-end fs-5 fw-bold text-primary">
                                @user.Score
                            </td>
                        </tr>
                        rank++;
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
