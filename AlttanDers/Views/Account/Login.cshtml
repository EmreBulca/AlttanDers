@{
    ViewData["Title"] = "Giriş Yap";
}

<div class="row justify-content-center mt-5">
    <div class="col-md-5">
        <div class="card glass-effect shadow-lg border-0 rounded-4 slide-up">
            <div class="card-header bg-transparent text-center py-4 border-bottom-0">
                <div class="mb-3">
                    <i class="bi bi-person-circle fs-1 text-primary"></i>
                </div>
                <h3 class="fw-bold mb-0 text-body">Tekrar Hoş Geldin! 👋</h3>
                <p class="mb-0 text-muted">Devam etmek için giriş yap</p>
            </div>
            <div class="card-body p-4 p-md-5 pt-2">

                @if (ViewBag.Error != null)
                {
                    <div class="alert alert-danger fade-in">
                        <i class="bi bi-exclamation-circle-fill me-2"></i> @ViewBag.Error
                    </div>
                }

                <form asp-action="Login" method="post">
                    <div class="mb-3">
                        <label class="form-label text-muted fw-bold small">E-POSTA ADRESİ</label>
                        <div class="input-group">
                            <span class="input-group-text bg-transparent border-end-0 text-primary"><i class="bi bi-envelope-fill"></i></span>
                            <input type="email" name="email" class="form-control border-start-0" placeholder="ornek@mail.com" required autofocus>
                        </div>
                    </div>

                    <div class="mb-4">
                        <label class="form-label text-muted fw-bold small">ŞİFRE</label>
                        <div class="input-group">
                            <span class="input-group-text bg-transparent border-end-0 text-primary"><i class="bi bi-key-fill"></i></span>
                            <input type="password" name="password" class="form-control border-start-0" placeholder="******" required>
                        </div>
                    </div>

                    <button type="submit" class="btn btn-primary btn-lg w-100 mb-3 rounded-pill fw-bold shadow-glow">
                        Giriş Yap <i class="bi bi-arrow-right-short"></i>
                    </button>
                </form>

                <div class="text-center mt-3">
                    <p class="text-muted small">Hesabın yok mu? <a asp-action="Register" class="text-decoration-none fw-bold text-primary">Hemen Kayıt Ol</a></p>
                </div>

            </div>
        </div>
    </div>
</div>
